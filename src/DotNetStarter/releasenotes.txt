Added netstandard 2.0 support.
Added StartupBuilder.Create() for easier configuration and startup.
Now throws exception if accessing DotNetStarter.ApplicationContext.Default during startup to avoid deadlock.